# 改进措施说明 (IMPROVEMENTS / ACM++)

本文件说明我在复现原论文之后提出的改进措施，合称 **ACM++**。
所有改进都是 **新增、可开关** 的：不开任何开关时，代码与原论文 ACM 完全等价；
"改进前 (baseline)" 与 "改进后 (ACM++)" 共用同一套代码，只是命令行 flag 不同，
因此对比是公平的。

---

## 1. 动机：原论文 ACM 的可改进点

原论文 ACM 的核心是 **非对称上下文调制**：
`Z = G(Y)⊗X + L(X)⊗Y`，其中 `G(Y)` 是自顶向下的全局通道注意力，
`L(X)` 是自底向上的点式通道注意力。它解决了"高层语义"与"低层细节"
跨层融合的问题。但仔细看，仍有三处可针对红外小目标进一步优化：

| # | 原论文的局限 | 改进措施 |
|---|---|---|
| 1 | ACM 全是**通道注意力**，没有显式的**空间定位**——哪一个像素是目标？ | 空间注意力门 (Spatial Gate) |
| 2 | 小目标在深层易被淹没，只有最终输出有监督，深层梯度弱 | 深监督 (Deep Supervision) |
| 3 | 纯 Soft-IoU 损失对**极端类别不平衡**(目标仅占 0.02% 像素)和暗弱目标不够敏感 | 混合损失 (Soft-IoU + Focal) |

---

## 2. 三项改进

### 改进 #1：空间注意力门 (Spatial Gate)

在 ACM 融合输出 `Z` 之后，接一个 CBAM 式空间注意力：

```
Z' = Z ⊗ σ(Conv7x7([AvgPool_c(Z); MaxPool_c(Z)]))
```

沿通道维做平均池化和最大池化得到 2×H×W，经 7×7 卷积压成 1×H×W 的空间权重图，
再用 sigmoid 归一化后逐像素加权。直观作用：**显式告诉网络"目标在哪个位置"**，
抑制背景杂波像素、增强小目标所在的少数像素。

- 代码：`src/models/acm.py` 的 `SpatialGate`，由 `CrossLayerFusion(spatial_gate=True)` 启用。
- 开销：每个融合模块仅 +98 参数（一个 7×7×2 卷积核）。

### 改进 #2：深监督 (Deep Supervision)

在解码器较粗的中间层（FPN 的 p2 / U-Net 的 d2）额外接一个辅助分割头，
对它也施加 Soft-IoU 监督，总损失为：

```
L = L_main + λ_aux · L_aux      (λ_aux = 0.4)
```

辅助头只在**训练时**生效（`model.training==True` 才返回 `[main, aux]`），
推理/评估时只用主头。作用：**让梯度更早、更直接地流到深层**，
缓解小目标特征在深层被稀释的问题。

- 代码：`src/models/networks.py` 中 `aux_head`，由 `deep_supervision=True` 启用。
- 开销：一个 1×1 卷积（+约 17~65 参数）。

### 改进 #3：混合损失 (Soft-IoU + Focal)

```
L_single = SoftIoU(p, t) + w_focal · Focal(p, t)      (w_focal = 1.0)
Focal = α·(1-p_t)^γ · CE ,  α=0.75, γ=2.0
```

Soft-IoU 保证分割整体形状，Focal 项专门给**难分类的少数前景像素**加权，
对暗弱、低对比目标更敏感。

- 代码：`src/metrics.py` 的 `FocalLoss` / `HybridLoss`。
- 开销：0 参数（仅损失函数）。

> 三项合计：ACM-FPN 仅 **+213 参数**（278,689 → 278,902），增幅 < 0.08%，
> ACM-U-Net **+229 参数**。改进几乎不增加模型体量，对比公平。
