## 单张图片检测指南 (INFERENCE)

本文件说明两件事：
1. 怎样用训练好的模型对**一张现实图片**做红外小目标检测（并对比"改进前 ACM vs 改进后 ACM++"）；
2. 这张现实图片应该**拍成什么样**，才能让模型有效检测。

脚本：`src/infer.py`。所有命令在项目根目录 `D:\Desktop\doc\research\ACM` 下执行。

---

### 1. 前置条件

需要先训练出权重（见 `HOWTO_RUN.md`）：

- 改进前（论文 ACM）：`runs/acm_fpn/best.pth`
- 改进后（ACM++）：`runs/acm_fpn_pp/best.pth`

> `acm_fpn_pp` 是开启三项改进（`--improved`）后自动生成的目录（带 `_pp` 后缀）。
> 训练命令见 `HOWTO_RUN.md` 第 4 节。

---

### 2. 改进前 vs 改进后对比（推荐）

把你的现实图片放到项目里（例如 `my_shot.png`），然后：

```bash
python src/infer.py --image my_shot.png \
    --net acm_fpn \
    --ckpt-before runs/acm_fpn/best.pth \
    --ckpt-after  runs/acm_fpn_pp/best.pth \
    --out results/compare_my_shot.png
```

产物 `results/compare_my_shot.png` 是一张 **2 行 × 4 列** 的对比图：

| | 列1 | 列2 | 列3 | 列4 |
|---|---|---|---|---|
| 第1行 | 输入灰度图 | 显著性热力图 | 二值掩膜 | 检测框（绿框） |
| 第2行（ACM++） | 同上 | 同上 | 同上 | 同上 |

终端还会打印每个模型检测到的目标数量与最大响应概率，便于直接量化对比。

---

### 3. 单模型检测

只想看一个模型的结果：

```bash
python src/infer.py --image my_shot.png --net acm_fpn --ckpt runs/acm_fpn/best.pth
```

产物：`results/detect_my_shot.png`（1 行 4 列：输入 / 热力图 / 二值掩膜 / 检测框）。

---

### 4. 常用参数

| 参数 | 默认 | 说明 |
|---|---|---|
| `--image` | 必填 | 现实图片路径（任意格式，内部转灰度） |
| `--net` | `acm_fpn` | 网络结构，需与权重一致（`acm_fpn` / `acm_unet`） |
| `--base-size` | 480 | 缩放到的正方形边长；设 `0` 保持原图尺寸 |
| `--thresh` | 0.5 | 二值化阈值。目标暗弱、漏检时调低到 0.3；杂波多、误检时调高到 0.6~0.7 |
| `--min-area` | 2 | 连通域最小面积（像素）。小于此值不算作目标，可滤掉孤立噪点 |
| `--out` | 自动 | 输出图片路径 |

> 网络结构、是否开启改进等会从 checkpoint 内保存的 `args` 自动读取，无需手动指定 `--spatial-gate` 等开关。

调参建议：先用默认值跑一次，看终端打印的 `max prob`。
- 如果 `max prob` 很高（>0.8）但漏检 → 适当调低 `--thresh`；
- 如果检测框很多、明显误检背景 → 调高 `--thresh` 或增大 `--min-area`。

---

### 5. 现实图片应该拍成什么样

模型是在 **SIRST 红外小目标数据集** 上训练的，输入会被转成单通道灰度。为了让检测有效，现实图片要尽量贴近训练分布。下面是拍摄/挑选要点。

#### 5.1 目标特征（最关键）

- **小**：目标在画面里只占很少像素。论文定义的小目标通常 < 图像面积的 0.15%，约几个到几十个像素。把相机离目标远一点，或目标本身就是远处的点状物。
- **点状 / 无明显纹理**：理想目标是一个亮点或小亮斑，没有可辨认的形状细节（如远处的飞机、无人机、信号灯、热源点）。
- **比背景亮（或暗）形成对比**：红外小目标的本质是"局部亮度异常"。目标与紧邻背景要有亮度差，但**不需要很强**——模型专门处理暗弱、低对比目标。

#### 5.2 背景

- **尽量均匀**：天空、海面、远山、平整地面这类大面积低频背景最理想。
- **少杂波**：避免树枝、栅栏、密集建筑、文字标牌等高频纹理——它们容易被误检成目标。
- **目标孤立**：目标周围一圈最好是干净背景，不要紧贴强边缘（如地平线、屋檐）。

#### 5.3 拍摄方式

- **优先红外/热成像**：如果有红外相机最好。没有也行——普通可见光照片会被转成灰度，仍可检测，但要满足"小、亮点、对比、干净背景"这几条。
- **构图**：目标放在画面中部，四周留足干净背景；不要把目标拍到画面边缘或角落。
- **曝光**：避免整体过曝/欠曝，保证目标和背景都在可分辨的灰阶范围内；不要让目标淹没在大片纯白或纯黑里。
- **对焦/抖动**：尽量清晰，避免运动模糊把点状目标拖成长条。
- **分辨率**：不必很高，脚本默认会缩放到 480×480。原图清晰即可。

#### 5.4 推荐场景举例

- 白天/黄昏的**天空中一架远处的飞机或无人机**（点状，天空均匀）。
- **路灯、信号灯、烟囱热源**等远处单个亮点，背景是天空或暗墙。
- 海面/湖面上**远处的一艘小船或浮标**。
- 雪地、沙地、草坪等均匀地面上的**单个远处小物体**。

#### 5.5 应避免

- 大而清晰、占据画面很大比例的主体（这不是"小目标"，模型未必擅长）。
- 杂乱背景（街景、室内堆物、密集人群）。
- 多个目标紧密重叠（连通域会被合并成一个框）。
- 严重模糊、过曝、噪点极多的图。

> 一句话：**远处一个清晰的小亮点，背景干净均匀**——越接近这个描述，改进前后的检测对比越能体现 ACM++ 的优势（更少误检、对暗弱目标更敏感）。

---

### 6. 把结果交给我

跑完对比后，把 `results/compare_my_shot.png`（以及终端打印的目标数 / max prob）给我，
我会把"改进前 vs 改进后"的单图检测结果写进实验报告，并用 `/make-pdf` 生成最终 PDF。
